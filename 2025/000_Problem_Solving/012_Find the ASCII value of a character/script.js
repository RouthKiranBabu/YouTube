let ch = 'A'; // Example input
let ascii = ch.charCodeAt(0); // Convert char to ASCII

console.log(`The ASCII value of '${ch}' is: ${ascii}`);
/*Explanation:
We declare a variable ch and assign it 'A'.
Using charCodeAt(0), we get the ASCII value of the first character.
We print the ASCII value.
Example Input & Output:
vbnet
Copy
Edit
Input: 'A'
Output: The ASCII value of 'A' is: 65*/